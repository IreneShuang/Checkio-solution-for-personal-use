def all_the_same(elements):
    count = 0 
    for i in range(0,len(elements)):
        if elements[i] == elements[0]:
            count += 1
        else: 
            count += 0
    if count == len(elements):
        return True
    else: return False
